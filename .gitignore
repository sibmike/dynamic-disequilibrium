# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
.env
.ipynb_checkpoints/

# Build outputs
build/
dist/

# Pytest / coverage
.pytest_cache/
.coverage
htmlcov/

# LaTeX build artefacts (paper/ is the canonical source tree)
paper/*.aux
paper/*.bbl
paper/*.blg
paper/*.log
paper/*.out
paper/*.toc
paper/*.fls
paper/*.fdb_latexmk
paper/*.synctex.gz
paper/sections/*.aux
# paper/main.pdf is tracked (so it can be downloaded directly from GitHub)

# Same for the arXiv submission tree
arxiv/edem/*.aux
arxiv/edem/*.bbl
arxiv/edem/*.blg
arxiv/edem/*.log
arxiv/edem/*.out
arxiv/edem/*.toc
arxiv/edem/*.fls
arxiv/edem/*.fdb_latexmk
arxiv/edem/*.synctex.gz
arxiv/edem/sections/*.aux
# arxiv/edem/main.pdf is tracked (matches paper/main.pdf, useful as a sanity check)

# The zip is regenerated by arxiv/edem/pack.sh
arxiv/edem_arxiv.zip

# Per-experiment Parquet datasets are regeneratable from the code;
# excluded to keep the repo lean. Figures (PDF/PNG) are tracked.
paper/figures/data/*.parquet

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Per-machine Claude Code state
.claude/
